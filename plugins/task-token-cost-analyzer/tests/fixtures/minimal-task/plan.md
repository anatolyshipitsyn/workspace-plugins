# Minimal Task Plan

## Goal

Define a package contract and safe evidence fixtures for an optional analyzer plugin.

## Steps

1. Write the package contract test first.
2. Add portable and Claude adapter manifests.
3. Add normalized event fixtures and minimal task evidence.
4. Run focused checks in normal and `-S` interpreter modes.
